# CourseBox 项目优化任务书

## 目标

在保留 FastAPI、SQLite 和原生 HTML/CSS/JavaScript 技术栈的前提下，将 CourseBox 从可运行的 MVP 优化为可维护、可扩展、可部署的课程资料平台。

## 执行约定

- 所有阶段都在当前仓库完成，不引入 Node/npm 或外部 CDN。
- 每个方案独立实现、独立验收，并形成一次 Git 提交。
- 每个方案完成后运行 `py -m pytest -q`，测试通过后提交并推送到 `origin/main`。
- 不覆盖或回滚已有用户改动；数据库和上传文件继续由 Git 忽略。
- API 继续保留中文公开路径，同时保留现有英文兼容路径，便于旧调用方迁移。

## 方案一：稳定性与安全基础

### 任务

- 统一数据库初始化和连接配置，启用 SQLite 外键、WAL、忙等待。
- 增加课程、资源、搜索相关索引。
- 把上传目录、数据库路径、单文件大小和允许扩展名纳入环境配置。
- 完善课程、标题、文件名、空文件和 MIME 类型校验。
- 保证文件写入和数据库写入的清理逻辑，避免孤儿文件或脏数据。
- 校验下载路径只能位于上传目录内。
- 增加错误场景测试和上传安全测试。

### 验收

```powershell
py -m pytest -q
py -c "from app.main import app; print(app.title)"
```

## 方案二：搜索、分页与数据管理

### 任务

- 课程、课程资源和搜索接口增加分页、总数和统一列表响应。
- 优先使用 SQLite FTS5 搜索，运行环境不支持时自动回退到 LIKE 搜索。
- 增加课程详情接口，详情页只依赖课程 ID。
- 增加课程编辑、课程删除、资源标题编辑和资源删除接口。
- 删除课程时同步删除关联磁盘文件。
- 保存文件 MIME 类型、SHA-256 和资源状态，检测同课程重复文件。
- 增加相关 API 测试。

### 验收

```powershell
py -m pytest -q
```

## 方案三：权限与内容治理

### 任务

- 增加用户、会话和角色模型。
- 提供登录、当前用户、退出登录和管理员创建用户接口。
- 支持 `admin`、`uploader`、`viewer` 三种角色。
- 课程创建/编辑/删除使用管理员权限，上传使用上传者或管理员权限。
- 非管理员上传进入待审核状态，管理员上传直接通过。
- 增加资源审核、拒绝和权限校验。
- 增加操作审计日志。
- 保持课程浏览、已通过资源浏览、搜索和下载可公开访问。

### 验收

```powershell
py -m pytest -q
```

## 方案四：前端完整体验

### 任务

- 首页增加登录状态、登录/退出、管理员课程创建入口。
- 首页接入分页课程列表、资源统计和搜索结果。
- 课程详情页从后端加载课程信息，不信任 URL 中的课程名称。
- 上传前显示文件信息，增加前端大小校验和上传进度。
- 展示文件类型、大小、上传时间和审核状态。
- 为管理员增加编辑/删除课程、删除/审核资源操作。
- 统一加载、空数据、错误、成功和权限提示。
- 完善移动端布局和键盘可访问性。

### 验收

```powershell
py -m pytest -q
py -m uvicorn app.main:app --port 8000
```

并人工访问 `http://127.0.0.1:8000/` 验证登录、建课、上传、搜索、审核和下载流程。

## 方案五：工程化与部署

### 任务

- 增加统一配置模块和 `.env.example`。
- 增加结构化应用日志和请求耗时日志。
- 健康检查同时验证数据库、上传目录和磁盘空间。
- 统一 API 错误响应格式，补充异常处理。
- 增加启动脚本、部署说明和备份恢复说明。
- 增加项目元数据和基础静态检查配置。
- 修复依赖弃用警告或明确锁定兼容版本。

### 最终验收

```powershell
py -m pytest -q
py -m compileall app
git status --short
```

## 提交记录

| 阶段 | 提交信息 | 状态 |
| --- | --- | --- |
| 方案一 | `完成方案一：稳定性与安全基础` | 已完成 |
| 方案二 | `完成方案二：搜索分页与数据管理` | 已完成 |
| 方案三 | `完成方案三：权限与内容治理` | 待完成 |
| 方案四 | `完成方案四：前端完整体验` | 待完成 |
| 方案五 | `完成方案五：工程化与部署` | 待完成 |
